<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bb.radio105">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.The105Zoo"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="m">
        <activity
            android:name="com.bb.radio105.SettingsActivity"
            android:label="@string/title_activity_settings">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.bb.radio105.MainActivity" />
        </activity>
        <activity
            android:name="com.bb.radio105.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.The105Zoo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.bb.radio105.MusicService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.bb.radio105.action.TOGGLE_PLAYBACK" />
                <action android:name="com.bb.radio105.action.PLAY" />
                <action android:name="com.bb.radio105.action.PAUSE" />
                <action android:name="com.bb.radio105.action.STOP" />
            </intent-filter>
        </service>

    </application>

</manifest>
