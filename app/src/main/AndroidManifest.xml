<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bb.radio105">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Radio105"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name="com.bb.radio105.MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.Radio105.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.bb.radio105.MusicService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.bb.radio105.action.PLAY" />
                <action android:name="com.bb.radio105.action.PAUSE" />
                <action android:name="com.bb.radio105.action.STOP" />
                <action android:name="com.bb.radio105.action.PLAY_NOTIFICATION" />
                <action android:name="com.bb.radio105.action.PAUSE_NOTIFICATION" />
                <action android:name="com.bb.radio105.action.STOP_NOTIFICATION" />
                <action android:name="com.bb.radio105.action.ERROR" />
            </intent-filter>
        </service>

    </application>

</manifest>
